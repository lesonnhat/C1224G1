let inputWidth = prompt('Nhập vào chiều dài: ');
let inputHeight = prompt('Nhập vào chiều rộng: ');

inputWidth = parseInt(inputWidth);
inputHeight = parseInt(inputHeight);

let area = inputWidth * inputHeight

// alert('Diện tích hình chữ nhật = ' + area);

document.write("The area is: " + area);